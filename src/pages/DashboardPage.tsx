import { DashboardView } from "@/features/analysis/components/DashboardView"

// Thin route component: the dashboard is composed entirely in the analysis feature.
export function DashboardPage() {
  return <DashboardView />
}
